version: '3'

services:
  bar:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../..:/bar/games/BAR.sdd:ro
      - ../../../../LuaUI/Widgets:/bar/LuaUI/Widgets:ro
      - cache:/bar/cache
volumes:
  cache:
